#pragma once

#include <stdint.h>

struct dma_pm_t;
typedef struct dma_pm_t dma_pm_t;

//dma_pm_t *dma_pm_create(void *from, int log_count, int log_bytes, int treq);

//void dma_pm_delete(dma_pm_t *dma);

//void *dma_pm_value(dma_pm_t *dma, uint32_t ir_offset);
